<p style="color: green"><%= notice %></p>

<%= render @blog %>

<div>
  <%= link_to "Edit this blog", edit_blog_path(@blog) if logged_in?(:admin) %> |
  <%= link_to "Back to blogs", blogs_path %>

  <%= link_to "Destroy this blog", blog_path, method: :delete if logged_in?(:admin) %>
</div>
